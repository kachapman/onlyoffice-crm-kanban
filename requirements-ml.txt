sentence-transformers>=2.2.0
scikit-learn>=1.2.0
numpy>=1.24.0
# NOTE: torch is installed separately in Dockerfiles via CPU-only index:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
# This avoids pulling nvidia/CUDA packages on CPU-only servers.
